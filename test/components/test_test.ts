import { renderComponent, jqComponent, expect } from '../test_helper';

describe('Test', () => {
  let component;
  let jqElement;

  beforeEach(() => {});

  it('knows how to sum', () => {
    expect(1 + 2).to.equal(3);
  });
});
