import { renderComponent, jqComponent, expect } from '../test-helper';

describe('Test', () => {
  let component;
  let jqElement;

  beforeEach(() => {});

  it('knows how to sum', () => {
    expect(1 + 2).to.equal(3);
  });
});
